# pingmailer-collection

1. Install bruno.
2. Find where the bruno folder is (Documents is usually default)
```
  cd <BRUNO-DEFAULT-FOLDER>
  git clone https://github.com/Silver-Mail-Platform/pingmailer-collection.git
```

3. Open it in Bruno
